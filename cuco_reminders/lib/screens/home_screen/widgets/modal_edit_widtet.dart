import 'package:flutter/material.dart';

class ModalEditWidtet extends StatelessWidget {
  const ModalEditWidtet({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
